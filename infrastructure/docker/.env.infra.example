# Infrastructure-level Docker Configuration

# Network Configuration
NETWORK_BACKEND=task_management_network
NETWORK_GATEWAY=task_management_gateway

# Volume Mount Paths (for production bind mounts)
POSTGRES_DATA_PATH=./infrastructure/data/postgres
RABBITMQ_DATA_PATH=./infrastructure/data/rabbitmq

# Resource Limits (apply per service in docker-compose)
DEFAULT_MEMORY_LIMIT=512m
DEFAULT_CPU_LIMIT=1.0

# Restart Policy
DEFAULT_RESTART_POLICY=unless-stopped